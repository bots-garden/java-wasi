# java-wasi

```bash
curl -G https://start.vertx.io/starter.zip -d "groupId=garden.bots" -d "artifactId=starter" -d "vertxVersion=4.3.3" -d "vertxDependencies=vertx-web" -d "language=java" -d "jdkVersion=11" -d "buildTool=gradle" --output starter.zip
```